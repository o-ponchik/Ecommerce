import React from "react";

import Link from "next/link";
import { urlFor } from "../lib/client";

const Product = ({ product: { imge, name, slug, price } }) => {
  return <div>Product</div>;
};

export default Product;
